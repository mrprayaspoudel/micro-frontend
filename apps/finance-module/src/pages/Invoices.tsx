import { PageContainer, PageTitle } from '@shared/ui-components';

const Invoices = () => {
  return (
    <PageContainer>
      <PageTitle>Invoice Management</PageTitle>
      <p>Invoice creation and management interface will be displayed here.</p>
    </PageContainer>
  );
};

export default Invoices;
